{
  "name": "vox-desktop",
  "version": "0.2.1",
  "description": "Vox desktop companion with a secure local Codex bridge",
  "author": "Vox",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "check": "node scripts/check.mjs && node --test scripts/computer-use-session.test.mjs && node scripts/personal-api.test.mjs && node scripts/personal-route.test.mjs",
    "prepare:web": "npm --prefix ../.. run build",
    "package": "electron-builder --dir",
    "dist": "npm run prepare:web && electron-builder"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.155.1"
  },
  "devDependencies": {
    "electron": "44.4.3",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.ericcheng.vox.desktop",
    "productName": "Vox",
    "asar": true,
    "asarUnpack": [
      "node_modules/@openai/codex-*/vendor/**/*"
    ],
    "files": [
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../dist",
        "to": "vox-web",
        "filter": ["**/*"]
      }
    ],
    "mac": {
      "icon": "resources/icon.icns",
      "category": "public.app-category.productivity",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": true,
      "entitlements": "resources/entitlements.mac.plist",
      "entitlementsInherit": "resources/entitlements.mac.plist",
      "extendInfo": {
        "NSCameraUsageDescription": "Vox uses the camera only when you enable visual conversation.",
        "NSMicrophoneUsageDescription": "Vox uses the microphone for private voice conversations."
      }
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "category": "Utility",
      "target": "AppImage"
    }
  }
}
