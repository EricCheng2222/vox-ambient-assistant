{
  "name": "vox-desktop",
  "version": "0.3.0",
  "description": "Vox desktop companion with secure local Codex and smart-home bridges",
  "author": "Vox",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "check": "node scripts/check.mjs && node --test scripts/computer-use-session.test.mjs scripts/local-web-server.test.mjs scripts/dyson-local.test.mjs scripts/smart-home-hub.test.mjs && node scripts/personal-api.test.mjs && node scripts/personal-route.test.mjs",
    "prepare:web": "npm --prefix ../.. run build",
    "package": "electron-builder --dir",
    "dist": "npm run prepare:web && electron-builder"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.155.1",
    "bonjour-service": "^1.4.4",
    "mqtt": "^5.16.0"
  },
  "devDependencies": {
    "electron": "44.4.3",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.ericcheng.vox.desktop",
    "productName": "Vox",
    "asar": true,
    "asarUnpack": [
      "node_modules/@openai/codex-*/vendor/**/*"
    ],
    "files": [
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../dist",
        "to": "vox-web",
        "filter": [
          "**/*"
        ]
      }
    ],
    "mac": {
      "icon": "resources/icon.icns",
      "category": "public.app-category.productivity",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": true,
      "entitlements": "resources/entitlements.mac.plist",
      "entitlementsInherit": "resources/entitlements.mac.plist",
      "extendInfo": {
        "NSCameraUsageDescription": "Vox uses the camera only when you enable visual conversation.",
        "NSMicrophoneUsageDescription": "Vox uses the microphone for private voice conversations.",
        "NSLocalNetworkUsageDescription": "Vox connects directly to smart-home devices that you configure on your local Wi-Fi network.",
        "NSBonjourServices": [
          "_dyson_mqtt._tcp"
        ]
      }
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "category": "Utility",
      "target": "AppImage"
    }
  }
}
